@model List<CategoryPj>

 <div class="list-group list-group-horizontal mb-4 mt-3">
    <a href="/projects" class="list-group-item list-group-item-action">
       <i class="fa-solid fa-list"></i> Tüm Kategoriler
    </a>

    @foreach (var item in Model)
    {
        <a asp-controller="Project"
        asp-action="List" 
        asp-route-category="@item.Url" 
        class="list-group-item list-group-item-action @(ViewBag.SelectedProjectCategories == item.Url?"active":"")">
            @item.Name
        </a>
    }
</div>   